# Copy to .env for local overrides (do not commit .env)

# Data: bundled deploy/data is used automatically if unset
# DATA_DIR=D:\purplle

# API
PORT=8000
CORS_ORIGINS=*
SEED_DEMO_DATA=true

# Pipeline (local CCTV processing only)
# CLIP_REFERENCE_DATE=2026-04-10
# STAFF_DETECTION_ENABLED=true
# API_BASE=http://localhost:8000
